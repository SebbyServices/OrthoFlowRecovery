.DS_Store
*.log
.vscode/
.idea/

# Private working notes, never committed. This repo is public.
PRIVATE_NOTES.local.md

# Commercial paperwork (agreements, proposals, invoices) has landed directly in this
# folder by habit at least once: a signed agreement PDF sat here untracked for a day
# before anyone noticed, one `git add .` away from going public. This repo has no
# legitimate use for a PDF or Word doc, so blanket-ignore them. Real client paperwork
# lives in the ops folder outside any repo, never here.
*.pdf
*.docx

# Local-only visual mockup of draft copy from CONTENT_DECK.md, rendered into the real
# page shell so it can be seen and tested in a browser instead of read as a spec. This
# is NOT the tracked preview/ deliverable and must never be committed: the signed
# agreement (Section 1.2.3) requires John's written per-line approval before any real
# copy publishes, and preview/ is the actual client-facing review venue under Section
# 7.2. draft-preview/ exists only so Sebby can look at layout and mobile behavior with
# real-shaped text in place of [placeholder], nothing in it is approved.
draft-preview/
